<h1 class="text-center mt-4">Edit '<%= @category.name %>' category name</h1>

<div class="container">
  <div class="row justify-content-center">
    <div class="col-10">
      <%= render 'form' %>
    </div>
    <div class="col-md-3 text-center mb-3">
      <%= link_to '[ Cancel and return to category ]', category_path(@category), id: "new-form-color-cancel" %>
    </div>
  </div>
</div>